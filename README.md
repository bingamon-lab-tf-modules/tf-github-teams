# tf-github-teams

## Overview

A Terraform Module for GitHub Teams.

This module also grants the organization `security_manager` role to a team via
`github_security_manager_team_slug`, and therefore supersedes the deprecated
`tf-github-secmgr` module.

The [Terraform Module](module/README.md) documentation contains the available variables and outputs.
